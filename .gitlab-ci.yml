image: node:latest

build:
  stage: build
  script:
    - echo "$NPMRC" > .npmrc
    - yarn install --frozen-lockfile
    - yarn build
  artifacts:
    paths:
      - dist
    expire_in: 1 week
  cache:
    paths:
      - node_modules/
